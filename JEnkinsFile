pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                ansiblePlaybook credentialsId: 'JenkinsItops', installation: 'Ansible', inventory: 'hosts.ini', playbook: 'pop_up_message.yml', vaultTmpPath: ''
            }
        }
    }
}