pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                ansiblePlaybook becomeUser: 'itops', credentialsId: 'itops', installation: 'Ansible', inventory: 'hosts.ini', playbook: 'install-google-chrome.yml', sudoUser: 'itops', vaultTmpPath: ''
            }
        }
    }
}
